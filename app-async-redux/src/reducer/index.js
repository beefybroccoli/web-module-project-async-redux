const initialState = {
  loading: false,
  data: null,
  isFetching: false,
  error: null,
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    /* case - fetch start */

    /* case - fetch success */

    /* case - fetch fail */
    default:
      return state;
  }
};
